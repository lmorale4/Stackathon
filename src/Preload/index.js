const idle = require('./Idle');
const run = require('./Run');
const jumpImages = require('./Jump');
const singleKnife = require('./Kunai');

module.exports = {
  idle,
  run,
  jumpImages,
  singleKnife,
};
